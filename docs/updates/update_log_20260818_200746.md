# Update Log / 更新日志

## Files Modified or Created / 修改或创建的文件

- Created / 创建：`outputs/runs/20260818_200609_test_map1_clean_main_pipeline`
- Created / 创建：`outputs/runs/20260818_200644_test_map1_clean_main_pipeline`
- Created / 创建：`outputs/runs/20260818_200720_test_map1_clean_material_generation`
- Created / 创建：this bilingual validation log / 本双语验证日志
- No production source files were modified during this validation / 本次验证未修改生产源码文件。

## Logic Updated or Steps Executed / 更新的逻辑或执行的步骤

- Ran the public full plan-only dry-run for `test_map1_clean`.
- 运行了 `test_map1_clean` 的公开完整计划 dry-run。
- Ran the existing internal material-generation dry-run entry without external backends to inspect the runtime artifact.
- 使用现有内部材质 dry-run 入口，在不调用外部后端的情况下检查运行时 artifact。
- Confirmed `prompt_llm_input_v3.json` keeps its own envelope version while its nested output target is `material_prompt_briefs_v4`.
- 确认 `prompt_llm_input_v3.json` 保持自身输入封装版本，同时内部输出目标为 `material_prompt_briefs_v4`。

## Test Results and Pending Next Steps / 测试结果与后续计划

- Public full dry-run passed with `llm_calls=0`, `sd_webui_generation_calls=0`, and `stablematerials_generation_calls=0`.
- 公开完整 dry-run 通过，`llm_calls=0`、`sd_webui_generation_calls=0`、`stablematerials_generation_calls=0`。
- Runtime metadata verification passed: `schema_version=prompt_llm_input_v3` and `output_schema.schema_version=material_prompt_briefs_v4`.
- 运行时 metadata 验证通过：`schema_version=prompt_llm_input_v3` 且 `output_schema.schema_version=material_prompt_briefs_v4`。
- The internal material dry-run stopped at the existing template validator because its wood first tag did not match the exact identity-token rule; no LLM or image backend was called.
- 内部材质 dry-run 因现有模板 validator 的木材第一标签未满足精确身份词规则而停止；本次没有调用 LLM 或图片后端。
- This failure is separate from the v3/v4 metadata fix and was not changed in this validation.
- 该失败与 v3/v4 metadata 修复无关，本次未对其进行修改。
