# Optional local StableMaterials LCM backend dependencies.
# Install a PyTorch build matching the host CUDA/CPU setup first when needed.
torch>=2.1,<3
diffusers>=0.30,<1
transformers>=4.40,<5
accelerate>=0.30,<2
safetensors>=0.4,<1
